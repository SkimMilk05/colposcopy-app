<p id="notice"><%= notice %></p>

<h1>This page will be deleted</h1>
<p> click on image to select </p>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @images.shuffle.each do |image| %>
        <tr> 
            <%= image_tag(image.img_url, style: 'width:50%')%>
            <%= link_to 'Select', image %>
        </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Image', new_image_path %>

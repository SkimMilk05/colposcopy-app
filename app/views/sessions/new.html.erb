
<!--navbar-->
    <nav class="navbar fixed-top navbar-expand-sm bg-light justify-content-between">
      <!--logo-->
         <% if signed_in? %>
           <%= link_to(image_tag("ColPractLogo.png", style: "width:40px"), signed_in_root_url) %>
         <% else %>
           <%= link_to(image_tag("ColPractLogo.png", style: "width:40px"), general_path_url) %>
         <% end %>
    </nav>

<div id="clearance" class="sign-in">

   <div class="center-div">
      <h2><%= t(".title") %></h2>
   </div>

   <div class="center-div">
     <%= render partial: '/sessions/form' %>
  </div>

</div>
